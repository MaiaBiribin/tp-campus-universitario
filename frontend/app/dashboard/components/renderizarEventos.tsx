

export default function RenderizarEventos(){
       async function renderizarEventosDia() {
        const resp = await fetch("")
       }

       async function renderizarEventosSemana() {
         const resp= await
       }

     
}